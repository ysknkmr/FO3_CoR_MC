\section{From CNF}\label{sec: from cnf}
\begin{yoshiki}
    TODO:
\end{yoshiki}

\begin{theorem}\label{theorem: SETH CNF}
    For $k \ge 1$ (TODO: $\ge 3$ is reasonable?),
    if the "model checking problem" for $\fo^{k}$ \kl{sentences} can be decided in $\poly(\len{\fml}) \cdot n^{k - \varepsilon}$ time for some $\varepsilon > 0$,
    then "SETH" is false.
\end{theorem}
\begin{proof}
\end{proof}

% \begin{proof}
%     Given a \kl{boolean formula} $\fml$ in the "conjunctive normal form" ("CNF"),
%     Given a \fo^{3} \kl{sentence} $\fml$ in,
% \end{proof}